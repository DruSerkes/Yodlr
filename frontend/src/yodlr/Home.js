import React from 'react';
import { Typography } from '@material-ui/core';

const Home = () => {
	return (
		<div className="Home">
			<Typography variant="h1">Yodlr Design Challenge</Typography>
			{/* TODO add logic to check if user is logged in and display different components/text */}
		</div>
	);
};

export default Home;
